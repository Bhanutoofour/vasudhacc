import Link from "next/link";
import type { InventoryComparison } from "@/types/inventory";
export function MovementList({ title, items, positive }: { title: string; items: InventoryComparison[]; positive: boolean }) { return <section className="rounded-xl border border-slate-200/80 bg-white"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 className="text-sm font-semibold text-slate-800">{title}</h2><p className="mt-0.5 text-[11px] text-slate-400">Since yesterday</p></div><Link href="/inventory" className="text-xs font-semibold text-[#246552]">View all</Link></div><div className="divide-y divide-slate-100">{items.map((item) => <Link href={`/inventory/${encodeURIComponent(item.inventoryItemId)}`} key={`${item.inventoryItemId}-${item.locationId}`} className="flex items-center gap-3 px-5 py-3.5 hover:bg-slate-50"><div className="grid size-9 shrink-0 place-items-center rounded-lg bg-[#f2eee3] text-sm font-semibold text-[#8f7c45]">{item.productTitle.charAt(0)}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold text-slate-700">{item.productTitle}</p><p className="mt-0.5 text-[10px] text-slate-400">{item.variantTitle} · {item.sku ?? "No SKU"}</p></div><span className={`text-xs font-bold ${positive ? "text-emerald-600" : "text-red-600"}`}>{item.todayChange > 0 ? "+" : ""}{item.todayChange}</span></Link>)}</div></section>; }
