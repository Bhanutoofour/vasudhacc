import { Icon, type IconName } from "@/components/ui/icon";
import { formatQuantity } from "@/lib/comparison/inventory";
export function SummaryCard({ label, value, helper, icon, tone = "neutral" }: { label: string; value: number; helper: string; icon: IconName; tone?: "neutral" | "positive" | "warning" | "danger" }) { const tones = { neutral: "bg-slate-100 text-slate-600", positive: "bg-emerald-50 text-emerald-700", warning: "bg-amber-50 text-amber-700", danger: "bg-red-50 text-red-600" }; return <div className="rounded-xl border border-slate-200/80 bg-white p-5 shadow-[0_1px_2px_rgba(15,23,42,.03)]"><div className="flex items-start justify-between"><div><p className="text-xs font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold tracking-tight text-slate-900">{formatQuantity(value)}</p></div><div className={`grid size-9 place-items-center rounded-lg ${tones[tone]}`}><Icon name={icon} className="size-[18px]"/></div></div><p className={`mt-3 text-[11px] ${tone === "positive" ? "text-emerald-700" : tone === "danger" ? "text-red-600" : tone === "warning" ? "text-amber-700" : "text-slate-400"}`}>{helper}</p></div>; }
