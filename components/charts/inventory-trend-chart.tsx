"use client";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { DailyTotal } from "@/types/inventory";
export function InventoryTrendChart({ data }: { data: DailyTotal[] }) { return <div className="h-64 w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 12, right: 8, left: -20 }}><defs><linearGradient id="inventoryFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2b7561" stopOpacity={.22}/><stop offset="100%" stopColor="#2b7561" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#e8ecea" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="date" axisLine={false} tickLine={false} tick={{ fill: "#84908b", fontSize: 11 }} dy={8}/><YAxis axisLine={false} tickLine={false} tick={{ fill: "#84908b", fontSize: 11 }} domain={["dataMin - 40", "dataMax + 40"]}/><Tooltip contentStyle={{ border: "1px solid #e2e8e5", borderRadius: 10, fontSize: 12 }} formatter={(value) => [`${Number(value).toLocaleString("en-IN")} units`, "Inventory"]}/><Area type="monotone" dataKey="inventory" stroke="#20624f" strokeWidth={2.5} fill="url(#inventoryFill)" dot={{ fill: "white", stroke: "#20624f", strokeWidth: 2, r: 4 }}/></AreaChart></ResponsiveContainer></div>; }
