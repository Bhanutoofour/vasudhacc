import { InventoryTable } from "@/components/inventory/inventory-table";
import { Icon } from "@/components/ui/icon";
import { mockInventory } from "@/lib/mock/inventory";
export default function InventoryPage() { return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-medium text-[#2d725f]">Inventory</p><h1 className="mt-1 text-2xl font-semibold tracking-tight text-slate-900">Inventory intelligence</h1><p className="mt-1 text-sm text-slate-500">Compare stock across three daily snapshots and locations.</p></div><button className="flex h-9 items-center justify-center gap-2 rounded-lg border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-600"><Icon name="download" className="size-4"/>Export inventory</button></div><div className="rounded-lg border border-blue-100 bg-blue-50/60 px-4 py-3 text-xs text-blue-700"><strong>Phase A preview:</strong> this table uses typed mock data. Shopify remains disconnected until Phase B.</div><InventoryTable items={mockInventory}/></div>; }
